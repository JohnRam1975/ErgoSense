apiVersion: v1
kind: Service
metadata:
  name: ergosense-api
  namespace: ergosense
spec:
  type: ClusterIP
  selector:
    app: ergosense-api
  ports:
    - port: 3001
      targetPort: http
      name: http
